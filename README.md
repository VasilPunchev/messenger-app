# Messenger App

A simple JavaScript messenger application that lets the user post and refresh messages using a local REST API.

### Features
- Send messages (name + text)
- Refresh and display all messages
- Uses async/await and Fetch API

### Usage
Just open `index.html` and use the interface.

### API Endpoint
